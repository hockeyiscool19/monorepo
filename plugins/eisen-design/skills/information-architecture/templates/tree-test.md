# Tree test — <app>

Tree version: <sitemap commit or date> · Date: <YYYY-MM-DD> · Participants: n = <≥ 8 per task; ≥ 30 for
statistics> · Tool: <Optimal Workshop, UXtweak, Maze, or paper cards with a text-only tree>

Rules: tasks are written in the participant's words and never contain the label under test ("Where would you look
to see if the gateway is up?", not "Find Status"). Participants see only labels — no visuals, no search.

## Tasks and results

| # | Task (participant's words) | Correct node(s) | Success % (≥ 80) | Direct % (≥ 60) | Median s (≤ 20) | First click % correct (≥ 70) | Pass |
|---|---|---|---|---|---|---|---|
| T1 | | | | | | | |
| T2 | | | | | | | |
| T3 | | | | | | | |

- **Success:** the participant ended on a correct node. **Direct:** they got there without backtracking.
- **Overall pass:** every task ≥ 80 % success and no task below 60 %. Otherwise revise — labels first, then
  grouping, then depth — and retest the failed tasks with new participants.
- With fewer than 5 participants, report each failure as a finding to fix, not as a percentage.

## Findings

| Task | Wrong path taken by ≥ 2 people | Likely cause (label · grouping · depth) | Change to the tree |
|---|---|---|---|
| | | | |

## Card-sort inputs (if run first)

- Open sort: <n> participants; groups that ≥ 60 % of people formed: <list>
- Closed sort: <n> participants; categories with ≥ 70 % agreement: <list>; below 70 %: <list, with the rename or merge>
