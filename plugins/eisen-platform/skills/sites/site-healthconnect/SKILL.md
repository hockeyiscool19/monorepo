---
name: site-healthconnect
description: HealthConnect (healthconnect) on eisensoftware.com — live URLs, API base and health check, auth mode, repository and local checkout, current deployment, and curl smoke tests. Load when working on, deploying, debugging, smoke-testing or calling HealthConnect. Generated from registry/registry.json; do not edit by hand.
---
<!-- GENERATED by plugins/eisen-platform/skills/site-plugin/scripts/generate-site-skills.mjs from registry/registry.json (app "healthconnect"). Do not edit: change the registry entry and rerun the script. -->

# 🏋️ HealthConnect — `healthconnect`

Lifts, Garmin HRV and training data in one place.

Status `live` · tags `health`, `python`, `cloud-run` · platform path `/healthconnect` · routing `path-prefix`, ready: **true**.
Routing note: Verified 2026-09-23 through the Hosting rewrite: HTML, /healthconnect/assets/* and /healthconnect/health all 200. OAuth PUBLIC_URL on the service still points at the Cloud Run host (Phase 4).

## URLs

| What | URL | Notes |
|---|---|---|
| Platform (Firebase Hosting rewrite) | https://eisensoftware.com/healthconnect/ | canonical URL; the Hosting rewrite passes the full path through |
| Direct (Cloud Run `jordan-lifts`, us-central1, project `researcher-455022`) | https://jordan-lifts-382031913173.us-central1.run.app | always reachable; bypasses the platform host |

## API

- Base `https://jordan-lifts-382031913173.us-central1.run.app/healthconnect` · health `/health` → https://jordan-lifts-382031913173.us-central1.run.app/healthconnect/health
- Auth `cookie` — Browser-session auth: sign in through the browser first; with curl, reuse a saved cookie jar (`-b cookies.txt`). An unauthenticated call is redirected to sign-in or answered 401.
- Gateway route `https://eisensoftware.com/api/healthconnect/` will forward to the base above once `platform.gateway.enabled` is true (it is false today, so the route returns Hosting's 404).

## Repository and deployment

- Repository https://github.com/hockeyiscool19/garmin (branch `main`) · local checkout `~/projects/healthconnect`
- Deployed version `0.1.0`, by `manual` — no CI receipt yet: the `register-app` step has not reported a deploy (site-plugin rule 6)
- Service: `gcloud run services describe jordan-lifts --project researcher-455022 --region us-central1 --format 'value(status.latestReadyRevisionName,status.url)'`

## Smoke tests

```bash
curl -sS -o /dev/null -w '%{http_code}\n' https://jordan-lifts-382031913173.us-central1.run.app/healthconnect/   # direct host → 200
curl -sS https://jordan-lifts-382031913173.us-central1.run.app/healthconnect/health   # health → 200 JSON
curl -sS -o /dev/null -w '%{http_code}\n' https://eisensoftware.com/healthconnect/   # through the platform → 200
```

## How to interact

- Browser-session auth: sign in through the browser first; with curl, reuse a saved cookie jar (`-b cookies.txt`). An unauthenticated call is redirected to sign-in or answered 401.
- Every path the app serves lives under `/healthconnect` on both hosts (Hosting passes the full path through). Locally, run with the base path set and open `http://localhost:<port>/healthconnect/`.
- Source of truth: the `healthconnect` entry in `registry/registry.json` (URLs, status, api, repo); CI patches its `deployment` block after each production deploy. Edit the registry, then rerun `node plugins/eisen-platform/skills/site-plugin/scripts/generate-site-skills.mjs`.
